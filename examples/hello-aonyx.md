# Hello, Aonyx

Once the V1 binary ships, this directory will hold runnable examples:

- `quickstart.sh` — install + first conversation
- `import-codebase/` — ingest a repo into the memory palace
- `time-machine/` — query the palace "as of last month"
- `skill-author/` — write your first `SKILL.md`
- `mcp-bridge/` — connect Aonyx as an MCP client to Claude Code

For now, see [`.bmad/prd.md`](../.bmad/prd.md) for the planned UX.
