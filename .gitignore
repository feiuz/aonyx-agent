# Rust
/target
**/*.rs.bk
*.pdb

# IDE
.idea/
.vscode/
*.iml
*.swp

# OS
.DS_Store
Thumbs.db

# Aonyx runtime data
.aonyx/
*.db
*.db-journal
*.db-wal
*.db-shm

# Env / secrets
.env
.env.local
.env.*.local

# Claude Code local overrides (per-dev permissions)
.claude/settings.local.json

# Build / dist
dist/
*.log

# Test snapshots staged but unaccepted
**/snapshots/*.new

# Local fixtures
/tmp/
/scratch/

# scratch dumps (Read-tool workaround)
_scan.txt
_eb.txt
_pp.txt

# Site — versioned separately on the infra GitLab, NOT in this public agent repo.
# (It carries infra deploy configs; keep it out of the OSS repo.)
/site/

# Desktop auto-updater signing key (PRIVATE — goes in CI secrets, never commit)
desktop/.tauri-signing.key
desktop/.tauri-signing.key.pub
